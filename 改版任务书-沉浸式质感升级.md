# 改版任务书：沉浸式质感升级（结构不动）

> 本文档是改版的**唯一完整上下文**，供主会话派发 web-designer agent 时使用。
> 生成时间：2026-09-06。当前分支：`feature/immersive-polish`。

---

## 一、任务概述

对「中华文明探源工程」网站做**沉浸式质感升级**。

**核心原则：四大板块与导航完全保留**——时空探源 / 文化遗珍 / 探源社区 / 个人中心 的 Tab 结构、吸顶导航、各模块内容与功能（高德地图、canvas-confetti、AI 助手、全部文案与图片）一律不动。所有升级都在各板块**内部**进行：视觉质感 + 沉浸式交互。

参考气质：陕西历史博物馆官网（sxhm.com）的图文卡片质感、四字古风短语、数据展示（如"馆藏 1717950 件"式统计）——但**不照搬其信息架构**，只借鉴视觉与叙事手法。

## 二、Git 安全与还原手册（先读）

- 仓库根目录：`D:\codex\end`（网站代码在其下 `网页代码\`）
- 当前分支 `feature/immersive-polish`，所有改动只在此分支进行
- 标签 `backup-pre-redesign` 指向改版前已上线的状态
- GitHub Pages 只跟随 main 分支部署 → **改版期间线上网站完全不变**

| 意图 | 操作 |
|------|------|
| 本地完全还原（不满意时） | `git checkout main && git branch -D feature/immersive-polish` |
| 查看改版前版本 | `git stash`（如有未提交改动）后 `git checkout backup-pre-redesign`，看完 `git checkout feature/immersive-polish` |
| 验收通过后上线 | `git checkout main && git merge feature/immersive-polish && git push`（或用 `推送到GitHub.bat`） |
| 上线后反悔 | `git revert` 相关提交，或 `git reset --hard backup-pre-redesign` 后强制推送（需确认） |

## 三、项目现状盘点（已勘察，可直接信任）

### 技术栈
React 18 + Vite 6 + Tailwind 4 + shadcn/Radix（Figma Make 导出），代码在 `D:\codex\end\网页代码\`。

### 代码结构（关键！）
**全站业务代码集中在一个文件**：`网页代码/src/app/App.tsx`（5228 行），Tab 用 useState 切换（非路由）。内部组件及起始行号：

| 组件 | 行号 | 说明 |
|------|------|------|
| `Divider` / `GoldDiamond` / `CornerBrackets` | 1090 / 1096 / 1100 | 装饰元素 |
| `SectionLabel` | 1109 | 章节标签（英文小字） |
| `StarRating` | 1124 | 评分 |
| `ChapterDivider` | 1144 | 四字短语章节分隔 |
| `CinematicPrologue` | 1167 | 电影式序章（约 960 行，重头戏） |
| `HorizontalTimeline` | 2129 | 水平时间轴 |
| `ModuleTimeSpace` | 2213 | 时空探源板块（含高德地图，全文件 18 处 AMap 引用） |
| `ModuleCulturalTreasures` | 3349 | 文化遗珍板块 |
| `ModuleCommunity` | 3854 | 探源社区板块 |
| `ModulePersonal` | 4634 | 个人中心板块 |
| `AIAssistant` | 4886 | AI 助手浮窗 |
| `App` | 5045 | 主入口（Tab 定义在 5049-5054） |

其他：`src/app/components/ui/` 是 51 个 shadcn 组件（勿改）；`src/data/siteImages.ts` 是图片注册表；`src/styles/{theme,index,globals,tailwind,fonts}.css`；`src/imports/DESIGN.md` 是 Figma 设计稿说明。

### 主题色板（必须遵循，不换基调）
"Imperial Han" 深色博物馆风：
- `--background: #3C1A0C`、`--foreground: #F4E4CC`、`--primary: #C89640`、`--secondary: #9B3020`、`--muted: #5E3220`、`--border: rgba(200,150,64,0.28)`、`--radius: 0`
- 点缀色（DESIGN.md）：鎏金 `#c9a227`、绛红 `#8b1a1a`、绢米 `#f0e6cc`、玄枢黑 `#0d0905`、哑金 `#8b7a5e`
- 字体：Noto Serif SC（hero-display 96px）

### 图片资产（改版最大资本，当前利用率低）
`src/assets/images/` 下按 `黄河 / 长江 / 其他` 流域 → 遗址 → 图片三级组织（石峁、良渚、陶寺、二里头、牛河梁、双槐树等 20+ 遗址，含出土文物照与非遗活动照），另有 `src/assets/community/`（仰韶/红山/良渚 各 3 张）。**升级时要充分利用这些素材**（大图卡、画廊、深度查看）。

### 必须保持可用的功能
高德地图（`高德api/` 目录 + App.tsx 内 18 处引用）、canvas-confetti、AIAssistant、图片路径体系。

## 四、升级清单（按板块）

### 全局（所有 Tab 共享）
1. 滚动渐显系统：section 进入视口时淡入+轻微上移（IntersectionObserver 或轻量 hook，全站统一）
2. 青铜纹样/回纹装饰系统：分隔线、卡片边角、SectionLabel 强化（延续现有 CornerBrackets/GoldDiamond 语言）
3. 鎏金渐变标题：重要标题用金箔渐变文字（background-clip），层级拉开
4. `ChapterDivider` 四字短语强化：两侧回纹线 + 印章式首字
5. 按钮/卡片/链接微交互：hover 状态统一（金色描边亮起、轻微位移）

### 时空探源（`ModuleTimeSpace` + `CinematicPrologue`）
1. hero 区加 **1 个**轻量 WebGL 动效背景（react-bits，见下方技能指引；移动端/低端设备静态降级为现有深色渐变）
2. 地图⇄时间轴双向联动：点时间轴年代节点 → 地图飞行定位到对应遗址并高亮；点地图遗址标记 → 时间轴滚动到对应年代。高德地图已有 Marker/视野 API，做事件桥接
3. 章节式滚动叙事：序章之后的遗址介绍按"章节"推进（滚动驱动进度指示），复用现有 `ChapterDivider`
4. 标题入场可用逐字/逐词动效（轻量，勿滥用）

### 文化遗珍（`ModuleCulturalTreasures`）
1. 文物卡片：hover 3D 倾斜 + 高光扫过 + 图片慢 zoom；卡片金线描边亮起
2. 点击弹出**文物深度查看层**（用现有 shadcn Dialog）：大图 + 文物信息 + 所属遗址链接（图片已有，信息取现有数据，不编造文物描述）
3. 顶部加 CountUp 数字统计条：遗址数 / 文物数 / 年代跨度（数据从现有 siteImages.ts 与文案统计，不虚构）

### 探源社区（`ModuleCommunity`）
1. 图片画廊升级：hover 揭示说明文字、图片慢 zoom，瀑布流/不规则网格排布
2. 非遗文化卡片：hover 展开动效，图片+技艺名称排版强化

### 个人中心（`ModulePersonal`）
- 仅做质感统一（卡片描边、渐显、间距节奏），**不新增功能**

### 收尾（M4）
1. 按 impeccable 的 SKILL.md 逐条自查：视觉层级、对齐节奏、文案、空态、对比度
2. 无障碍：对比度达 WCAG AA、键盘可达、`prefers-reduced-motion` 时关闭动效
3. 性能：图片懒加载（loading="lazy"）、大图压尺寸、避免长任务阻塞
4. 移动端：WebGL 降级、触控目标 ≥44px、横向时间轴可滑动

## 五、技能使用指引（agent 照此执行）

| 技能 | 位置 | 用法 |
|------|------|------|
| ui-ux-pro-max | `C:\Users\刘振洲\.zcode\skills\ui-ux-pro-max\` | 先读其 SKILL.md；检索：`python scripts/search.py "<关键词>" --design-system`（新页面）、`--domain style|ux|landing`（定向）、`--stack react`（React 最佳实践）。Windows 用 `python` |
| react-bits | `C:\Users\刘振洲\.agents\skills\react-bits\` | 查 `reference\catalog.md` 选组件；安装 `npx shadcn@latest add "@react-bits/<组件名>-TS-TW"`。**优先轻量组件**（CountUp、SplitText、GlareHover、LogoLoop 等）；WebGL 类（Aurora、Particles 等）全站仅 hero 1 处 |
| impeccable | `C:\Users\刘振洲\.agents\skills\impeccable\` | M4 收尾时读其 SKILL.md，逐条对照审查 |
| karpathy-guidelines | `C:\Users\刘振洲\.agents\skills\karpathy-guidelines\` | 编码纪律：外科手术式修改——5228 行单文件只改必要处，不重写可用代码，每步可验证 |

## 六、硬性约束

1. **不增删 Tab、不重组模块、不换导航**；Tab 键名（探源/遗珍/社区/个人）与切换逻辑不动
2. 高德地图、canvas-confetti、AIAssistant 必须保持可用
3. 遵循现有 Imperial Han 色板与 radius 0 语言，不引入新色系
4. 性能预算：WebGL 全站 1 个、图片全部懒加载、避免一次性渲染超长列表
5. 不编造文物/遗址的史实描述——文案只用现有内容或从现有资料改写
6. 每完成一个里程碑必须提交一次 git commit（信息注明里程碑编号）

## 七、里程碑与验收流程

| 里程碑 | 内容 | 验收点 |
|--------|------|--------|
| M1 | 全局装饰系统 + 滚动渐显 + 标题升级 + 时空探源 hero/WebGL 背景 | 首屏质感、WebGL 可降级 |
| M2 | 文化遗珍（卡片/深度查看/CountUp）+ 探源社区画廊 | 交互手感、图片利用率 |
| M3 | 地图⇄时间轴联动 + 章节滚动叙事 | 联动正确性、地图功能无损 |
| M4 | impeccable 审查 + 无障碍 + 性能 + 移动端 | 对照清单逐项过 |

**验收方式**：每个里程碑完成后，主会话启动开发服务器（`cd /d/codex/end/网页代码 && npm run dev`，后台运行，地址 http://localhost:5173），**用户在浏览器实际过目确认后**才进入下一里程碑。最终（可选）可将页面渲染为 PNG 交 judge agent 做视觉验收。

## 八、给新会话主 agent 的执行指引

1. 确认 web-designer agent 可用（重启后已加载）；派发时附上本文档路径，并指明当前里程碑
2. 每个里程碑一次派发，不要一次性派发全部（用户要逐段验收）
3. 若 web-designer agent 因故不可用，备选方案：主会话直接按本文档执行（先读四个技能的 SKILL.md）
4. 改动全部发生在 `feature/immersive-polish` 分支；用户未合并前不得推送 main
