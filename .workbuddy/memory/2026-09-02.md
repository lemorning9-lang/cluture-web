# 2026-09-02

## 中华文明探源工程网站 — 本地启动与结构梳理

- 项目根目录：`D:\codex\end\网页代码`（React 18 + Vite 6 + TS + Tailwind v4）
- 启动方式（托管 Node）：`cd "D:/codex/end/网页代码" && C:/Users/刘振洲/.workbuddy/binaries/node/versions/22.22.2/npm.cmd run dev -- --host 127.0.0.1 --port 5173`
- 服务已验证可访问：http://127.0.0.1:5173/ 返回 HTTP 200，标题「中华文明探源工程」

### 后续改样式的关键入口（用户要求：字号整体加大、背景改明亮偏明黄）
- 全局字号总开关：`src/styles/theme.css` 的 `:root { --font-size: 16px }`，`html { font-size: var(--font-size) }` 依赖它，改一处即可全局放大
- 背景/主题色变量：同文件 `:root` 块，当前 `--background: #1E0C08`（深砖黑）、`--foreground: #F4E4CC`、`--primary: #C89640`（琥珀金）、`--secondary: #9B3020`（砖朱）
- 改亮色背景需同步处理：card/popover/muted/accent/sidebar 系列变量 + `--border` 与 `--ring`（现为半透明金色 rgba），否则文字对比度会崩
- 字体：`src/styles/fonts.css` 远程引入 Noto Serif SC /  Zhi Mang Xing / LXGW WenKai

### 结构备注
- `src/app/App.tsx` 为 5212 行单体主文件（早期总结中提到的 `src/data/` 拆分目前只剩 `data/siteImages.ts`，数据疑似已内联回 App.tsx）
- `src/app/components/` 下仅 `figma/` 与 `ui/` 两个目录
- 工作区另有 `本地资料数据库/`（135 png / 27 jpg / 6 docx）与 `探源社区/`（9 png）
- 部署脚本：`推送到GitHub.bat`；启动脚本：`启动网页.bat`

### 状态
本次只做启动 + 说明，未改动任何样式代码，等用户下命令再改。

## 修复：脚本启动报「网页代码错误」

- 现象：`启动网页.bat` 打开后页面报错，`http://127.0.0.1:5173/src/app/App.tsx` 返回 HTTP 500
- 根因：`网页代码/高德api/config.js` 缺失（含 API Key，曾被 commit `ca6e79f9` 移出 Git 跟踪并加入 .gitignore；线上由 GitHub Secrets 注入，本地未同步），App.tsx 第 32 行 `import AMAP_CONFIG from "../../高德api/config.js"` 解析失败
- 修复：按 `config.example.js` 模板重建 `高德api/config.js`（key 为占位符）。代码对占位 Key 有兜底：控制台 warn 并跳过地图加载，其余功能正常
- 验证：App.tsx 恢复 HTTP 200，页面正常渲染
- 注意：本地地图功能当前是禁用状态（占位 Key）；如需本地看真实地图，把真实 Key 填入 `网页代码/高德api/config.js` 即可；该文件不会进 Git

## 版本核查 + 亮色主题尝试与回滚

### 版本核查结论
- 本地 git 工作区干净，HEAD = a512a75e 与 origin/main 跟踪一致（代理连不上 GitHub 无法 fetch，但本地记录无领先/落后）
- Figma Make 旧版本残留（git 未跟踪、在磁盘上，共 11 个文件，约 9.5MB，都在 `网页代码/src/imports/`）：
  `____-1-1.jpg、____-2-1.jpg、____-3-1.jpg`（与跟踪的 ____-1/2/3.jpg 内容重复，大小相同）、`____-11___-____.pdf、____-11___-____-1.pdf、__7_______.pdf`（约 9MB PDF）、`code.html、code-1.html、code-2.html`（Figma 导出的旧代码）、`DESIGN.md、image.png`
  → 已向用户报告，等确认后再清理（回收站方式）
- 另 `网页代码/dist/`、根目录 `package-lock.json`、`.npm-cache/` 也未被跟踪

### 亮色主题尝试 → 用户不满意 → 已完全回滚
- 曾改为：--background #EDD9A4（明棕黄）、--primary #9A6A14、--foreground #3A2810、--font-size 16→17px；并加了 DARK_THEME_VARS 局部覆盖保住序章壁画深色；删除全部 7 处 boxShadow + 5 处 Tailwind shadow
- 用户反馈「颜色不对，还原」→ `git checkout -- theme.css App.tsx` 一键还原，已验证 #1E0C08 深色主题恢复、HTTP 200
- 教训：改主题前先出小样/色卡让用户确认色号，不要直接全套替换
- 高德api/config.js 修复未回滚（不在 git 内，属于必要的启动修复）

### 字体试效：序章正文与章节条目改楷体
- 用户新增 `const FS = "'KaiTi','STKaiti','楷体','Noto Serif SC', serif"`，把序章 3 段正文（源/数万年前/中华文明特质）与章节二、三两组条目的标题+描述（血脉相依/和衷共济/休戚与共/万川归海/礼制一统/文脉绵延）从 FD（Zhi Mang Xing 行楷）改为 FS
- 先试过宋体（SimSun），用户随后要求改为楷体；FS 常量现为 KaiTi 栈
- 英文小标题（Bound by Blood 等）保留 FE（Times）；装饰大字「源/汇」保留 FD
- 改动仅在 App.tsx，theme.css 未动；HTTP 200 编译通过；属试效，可能再回滚

### 字体分组 + 字色加深（经用户纠正）
- 序章 3 段正文：保留楷体（FS）；不透明度 /85、/70→加深为 /95、/85（用户未要求回退，保留）
- 六组章节条目（血脉相依…文脉绵延 标题+描述）：用户明确「字体不动应为楷体、颜色不变只调深浅」
  → 字体保持 FS（楷体）；描述色还原为 text-muted-foreground（不改 token）；仅标题透明度 /90→/95 做深浅微调
- 随后用户再定稿六组配色：小标题 text-primary（琥珀金 #C89640），描述 text-white（纯白）；字体仍楷体 FS
- 小标题色阶试调：#C89640 → #A8741E(深) → #E8B84B(亮金,定稿) 并加 textShadow 金色光晕（0 0 10/22/44px rgba(232,184,75,…)，类比旁侧大字「源/汇」的发光效果，按标题尺寸缩放）
- 常量：FS=楷体(序章+条目), FD=行楷(装饰大字), FE=Times(英文)；FSONG(宋体)常量已弃用但保留

## 样式更新：应用「新版本」文件夹的 Figma 新样式（方案A 壁炉暖焰）

- 用户要求：亮度、字号、卡片质感更新；图片和文字内容不变；更新属性从 `新版本/` 文件夹（重新放入的完整 Figma 导出项目）获取
- 做法theme.css 整文件复制；App.tsx 用 difflib 行级 diff 生成 190 个差异块后按规则合并补丁
- 补丁规则：含图片/地图标识符（siteImages/getSiteMainImage/COMMUNITY_IMAGES/AMap/lat/lng 等）的块保留旧版；地图结构块 81-85 保留旧版（旧版有 AMAP 条件渲染）；块 86 行级合并（保留 z-10、应用字号）；其余 149+1 块为纯样式替换全部应用
- 样式变更内容：底色提亮约1.5档（--background #1E0C08→#3C1A0C 等 9 个变量）；字号整体升一档（text-xs→text-sm、text-sm→text-base、text-base→text-2xl、text-xl→text-3xl、text-3xl→text-4xl、text-[9px]/[11px]→text-xs）；渐变遮罩减淡（/70→/40 等）；暗角 rgba 0.75-0.82→0.12；卡片质感色 rgba(200,165,110,0.13)→rgba(155,48,32,0.18)；阴影减小；抽屉 w-80→w-[420px]
- 验证：vite build 无错误；中文内容与旧版逐段比对完全一致（1080 段）；125 张本地图片抽查 6 张全部 200；theme.css 服务端确认新值；样式特征 grep 全部命中
- 备份：补丁前 App.tsx 备份于 `.workbuddy/App.tsx.backup-before-style-patch`；补丁/分析脚本在 `.workbuddy/`（apply_patch.py、classify_blocks.py 等）
- 结果：总行数保持 5213 不变（纯行内替换），图片系统与地图代码原样保留
